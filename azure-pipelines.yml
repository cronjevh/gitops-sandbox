resources:
  containers:
  - container: custom
    image: ghcr.io/cronjevh/azure-devops-runner:0.0.4

container: custom

trigger:
- master

pool: 'Azure Pipelines'

steps:

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'

- task: Docker@2
  inputs:
    command: 'build'
    Dockerfile: 'deploy/pipeline/azure-devops-image/Dockerfile'